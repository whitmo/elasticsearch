# This config is autogenerated by ansible.
# See templates/elasticsearch.yml in the charm folder.

cluster.name: {{ cluster_name }}
discovery.zen.ping.multicast.enabled: false
discovery.zen.ping.unicast.hosts:
{% for reln in relations.peer.values() %}
  {%- for unit_id, reln_data in reln.items() -%}
    {% if unit_id != local_unit %}
  - {{ reln_data['private-address'] }}
    {% endif %}
  {%- endfor -%}
{% endfor %}
